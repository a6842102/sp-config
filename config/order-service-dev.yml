spring:
  application:
    name: order-service
#设置禁止配置中心的配置将客户端
  cloud:
    config:
      override-none: true
server:
  port: 8201
#服务提供者向eureka注册信息
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka, http://eureka2:2002/eureka
#feign启动hystrix降级，熔断
feign:
  hystrix:
    enabled: true
#暴露Hystrix监控
management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream
